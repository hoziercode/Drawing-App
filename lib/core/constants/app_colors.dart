import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF0E0F12);
  static const surface = Color(0xFF15171B);
  static const accent = Color(0xFF786CFF);
  static const glow = Color(0x885A4CFF);
  static const white = Colors.white;
}
